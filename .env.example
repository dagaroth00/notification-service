# Example environment variables for notification-service
# Server
PORT=3000

# Database (Prisma)
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Redis (if used by socket adapter or caching)
REDIS_URL=redis://localhost:6379

# JWT / Auth (if using auth middleware)
JWT_ISSUER=
JWKS_URI=

# Logging
LOG_LEVEL=info

# Request-level logging controls
# Set to 'false' to disable per-request logging (default enabled)
REQUEST_LOG_ENABLED=true
# Optional: override request log level (info|debug|warn|error)
REQUEST_LOG_LEVEL=info

# Other optional settings
# SOCKET_REDIS_HOST=localhost
# SOCKET_REDIS_PORT=6379
